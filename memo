=IF(
  AND(マトリクス!$E6<>"",
      COUNTIFS(
        テストケース・結果一覧!$BC:$BC, マトリクス!F$3,
        テストケース・結果一覧!$AG:$AG,
        "*"&SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(マトリクス!$E6,"~","~~"),"*","~*"),"?","~?")&"*"
      )>0
  ),
  "○","-"
)