コード修正して以下入れてみたんですが、まだ空のままです。
元テーブルpcm_t_景表法遵法チェック結果_総付景品.総付景品2_2には"A-1"という値が入っているデータが存在しています。インポートは問題ないです。

IF(IFALL(LEFT({pcm_t_景表法遵法チェック結果_総付景品.総付景品2_2},1)="A",RIGHT({pcm_t_景表法遵法チェック結果_総付景品.総付景品2_2},1)="1"),"A-1","")
